import styled from 'styled-components';

const AuthInput = styled.input`
  width: 100%;
  height: 60px;
  background-color: transparent;
  border: none;
  color: var(--white);

  &:focus {
    outline:none;
  }
`

export {AuthInput}
